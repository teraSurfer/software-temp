import React, { Fragment } from 'react';
import Router from '../router';
import './app.scss';

const App = (props: any) => {
  return (
    <Fragment>
      <Router />
    </Fragment>
  );
};

export default App;
